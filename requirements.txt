# Public Transport Delay — Python dependencies
# Install: pip install -r requirements.txt
#
# --- Not installed by pip (install separately) ---
# • Power BI Desktop: https://www.microsoft.com/power-platform/products/power-bi/
#   Used for: power bi/public_transport_delay.pbix dashboards and reports.
# • MySQL + MySQL Workbench: https://dev.mysql.com/downloads/
#   Used for: running SQL/transport_delay_queries.sql and receiving data from ETL/Load.ipynb.

# Core data & ML
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# SQL / database (ETL/Load.ipynb → MySQL Workbench)
# Connection string: mysql+pymysql://user:password@host/database
SQLAlchemy>=2.0.0
PyMySQL>=1.1.0

# Visualization (EDA, Model notebook)
matplotlib>=3.7.0
seaborn>=0.13.0

# Explainability (Model notebook)
shap>=0.44.0

# Web app
streamlit>=1.28.0

# Jupyter notebooks (ETL, EDA, Model)
jupyter>=1.0.0
ipykernel>=6.25.0

# Optional: Power BI REST API / Microsoft 365 automation (only if you script reports)
# Uncomment if needed:
# msal>=1.24.0
# requests>=2.31.0
